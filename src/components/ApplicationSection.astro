---
---

<section id="apply" class="application-section">
  <div class="container">
    <div class="section-header animate-fade-up">
      <span class="section-badge">ONLINE APPLICATION</span>
      <h2 class="section-title">กรอกใบสมัคร</h2>
      <p class="section-subtitle">
        กรุณาตรวจสอบข้อมูลและตำแหน่งที่สนใจก่อนเริ่มกรอก แบบฟอร์มจะแสดงคำถามเฉพาะทีมที่เลือกเป็นอันดับที่ 1
      </p>
    </div>

    <div class="form-embed-box animate-fade-up" style="animation-delay: 150ms;">
      <div style="position: relative; height:80dvh; overflow:auto;">
        <iframe
          src="https://acadform.medcmu69.com/s/cmri3v7ub000d01mswvr4ywur?embed=true"
          frameborder="0" style="position: absolute; left:0; top:0; width:100%; height:100%; border:0;">
        </iframe>
      </div>
    </div>
  </div>
</section>

<style>
  .application-section {
    padding: 5rem 0 6rem;
    background: linear-gradient(180deg, var(--bg-page) 0%, var(--bg-subtle) 100%);
    border-top: 1px solid var(--border-subtle);
  }

  .section-header {
    max-width: 48rem;
    margin: 0 auto 2.75rem;
    text-align: center;
  }

  .section-badge {
    display: inline-block;
    font-size: 0.8125rem;
    font-weight: 700;
    color: var(--accent);
    background: var(--accent-indigo-light);
    padding: 0.35rem 0.95rem;
    border-radius: var(--radius-full);
    margin-bottom: 0.875rem;
    letter-spacing: 0.05em;
  }

  .section-title {
    font-size: 2rem;
    color: var(--primary);
    margin-bottom: 1rem;
    word-break: break-word;
  }

  @media (min-width: 768px) {
    .section-title {
      font-size: 2.45rem;
    }
  }

  .section-subtitle {
    font-size: 1.0625rem;
    color: var(--text-muted);
    line-height: 1.75;
    word-break: break-word;
  }

  .form-embed-box {
    max-width: 68rem;
    margin: 0 auto;
    background: #ffffff;
    border: 1px solid var(--border-subtle);
    border-radius: var(--radius-lg);
    box-shadow: var(--shadow-lg);
    overflow: hidden;
    width: 100%;
  }
</style>
